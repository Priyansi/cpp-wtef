#include <algorithm>
#include <iostream>
#include <utility>

using namespace std;

int maxSumFromEndsTillK(int arr[], int size) {
}

int main() {
    int size = 6;
    int arr[size] = {1, 2, 3, 4, 5, 6};
    cout << maxSumFromEndsTillK(arr, size);
}